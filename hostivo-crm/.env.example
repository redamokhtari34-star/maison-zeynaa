# URL et clé publique (anon) du projet Supabase — Project Settings > API.
# Sans ces deux valeurs, l'app reste en mode démo (données d'exemple, lecture seule).
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
